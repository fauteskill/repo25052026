# This is a basic workflow that is manually triggered

name: Deploy Manual nosso Site

# Controls when the action will run. Workflow runs when manually triggered using the UI
# or API.
on:
  workflow_dispatch:
    # Inputs the workflow accepts.
    inputs:
      substituir:
        type: boolean
      regiao:
        description: 'Escolhe uma opção'
        type: choice
        options:
          - Adicionar
          - Pesquisar
          - Listar todos
      ambiente:
        type: environment
      servidor:
        description: 'Nome do servidor'
      ip:
        description: 'IP do servidor'
      grupo:
        description: 'Grupo do servidor'
      pasta:
        # Friendly description to be shown in the UI instead of 'name'
        description: 'Pasta de destino'
        # Default value if no value is explicitly provided
        default: '/var/www'
        # Input has to be provided for the workflow to run
        required: true
        # The data type of the input
        type: string

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "greet"
  copia:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
    # Runs a single command using the runners shell
    - uses: actions/checkout@v4
    
    - name: passo1
      run: echo "Hello ${{ inputs.servidor }}"
    - name: passo2
      run: |
        ls -all
        chmod 700 exemplo.sh
        ./exemplo.sh -a ${{ inputs.servidor }} ${{ inputs.ip }} ${{ inputs.grupo }} 
        cat hosts.yml
      

      
